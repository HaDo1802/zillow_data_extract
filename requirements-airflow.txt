# Docker-only: installed into the Airflow base image with Airflow's constraint URL.
# Local dev uses pyproject.toml + uv instead — do not add packages here manually,
# keep this in sync with the [project.dependencies] section of pyproject.toml.
pandas>=1.5.0
numpy>=1.24.0
requests>=2.28.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
pydantic>=1.10.0
PyYAML>=6.0
boto3==1.34.106
